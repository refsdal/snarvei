// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: invitations.sql

package gen

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const getInvitation = `-- name: GetInvitation :one
SELECT i."id", i."organization_id", i."email", COALESCE(i."roles", '') AS roles, i."status",
    i."expires_at", i."created_at",
    o."name" AS organization_name, o."slug" AS organization_slug,
    COALESCE(u."name", '') AS inviter_name,
    it."team_id" AS team_id, t."name" AS team_name
FROM "organization_invitations" i
JOIN "organizations" o ON o."id" = i."organization_id"
LEFT JOIN "users" u ON u."id" = i."inviter_id"
LEFT JOIN "invitation_teams" it ON it."invitation_id" = i."id"
LEFT JOIN "teams" t ON t."id" = it."team_id"
WHERE i."id" = $1
`

type GetInvitationRow struct {
	ID               string
	OrganizationID   string
	Email            string
	Roles            string
	Status           string
	ExpiresAt        pgtype.Timestamptz
	CreatedAt        pgtype.Timestamptz
	OrganizationName string
	OrganizationSlug string
	InviterName      string
	TeamID           *string
	TeamName         *string
}

// The public view plus everything accept/register need. The inviter join is
// LEFT because "inviter_id" is nullable; the team join is LEFT so a deleted
// team never hides the invitation. "organization_invitations.inviter_id" is
// ON DELETE CASCADE, so deleting an admin removes their pending invitations.
func (q *Queries) GetInvitation(ctx context.Context, id string) (GetInvitationRow, error) {
	row := q.db.QueryRow(ctx, getInvitation, id)
	var i GetInvitationRow
	err := row.Scan(
		&i.ID,
		&i.OrganizationID,
		&i.Email,
		&i.Roles,
		&i.Status,
		&i.ExpiresAt,
		&i.CreatedAt,
		&i.OrganizationName,
		&i.OrganizationSlug,
		&i.InviterName,
		&i.TeamID,
		&i.TeamName,
	)
	return i, err
}

const listPendingInvitations = `-- name: ListPendingInvitations :many
SELECT i."id", i."email", COALESCE(i."roles", '') AS roles, i."status", i."expires_at", i."created_at",
    it."team_id" AS team_id, t."name" AS team_name
FROM "organization_invitations" i
LEFT JOIN "invitation_teams" it ON it."invitation_id" = i."id"
LEFT JOIN "teams" t ON t."id" = it."team_id"
WHERE i."organization_id" = $1 AND i."status" = 'pending'
    AND (i."expires_at" IS NULL OR i."expires_at" > now())
ORDER BY i."created_at" DESC
`

type ListPendingInvitationsRow struct {
	ID        string
	Email     string
	Roles     string
	Status    string
	ExpiresAt pgtype.Timestamptz
	CreatedAt pgtype.Timestamptz
	TeamID    *string
	TeamName  *string
}

func (q *Queries) ListPendingInvitations(ctx context.Context, organizationID string) ([]ListPendingInvitationsRow, error) {
	rows, err := q.db.Query(ctx, listPendingInvitations, organizationID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []ListPendingInvitationsRow
	for rows.Next() {
		var i ListPendingInvitationsRow
		if err := rows.Scan(
			&i.ID,
			&i.Email,
			&i.Roles,
			&i.Status,
			&i.ExpiresAt,
			&i.CreatedAt,
			&i.TeamID,
			&i.TeamName,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const setInvitationTeam = `-- name: SetInvitationTeam :exec
INSERT INTO "invitation_teams" ("invitation_id", "team_id") VALUES ($1, $2)
ON CONFLICT ("invitation_id") DO UPDATE SET "team_id" = EXCLUDED."team_id"
`

type SetInvitationTeamParams struct {
	InvitationID string
	TeamID       string
}

func (q *Queries) SetInvitationTeam(ctx context.Context, arg SetInvitationTeamParams) error {
	_, err := q.db.Exec(ctx, setInvitationTeam, arg.InvitationID, arg.TeamID)
	return err
}
